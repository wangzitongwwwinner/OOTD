# 穿衣有数｜测试规范

> 测试从 [PRODUCT.md](./PRODUCT.md) 的验收事实、[DESIGN.md](./DESIGN.md) 的交互规范、飞书页面说明的页面状态和 [ARCHITECTURE.md](./ARCHITECTURE.md) 的技术约束生成，不以“当前代码能做什么”为测试边界。

微信开发者工具、真机、体验版和发布测试同时遵循 [WECHAT_MINIPROGRAM.md](./WECHAT_MINIPROGRAM.md)。

指标计算、评价去重和报告测试同时遵循 [METRICS.md](./METRICS.md)。

## 1. 目标与原则

- 主链路、边界条件、权限拒绝、外部失败、并发和状态恢复同等重要。
- 行为变更必须先有能失败的测试，再写最小实现。
- 测试环境不得调用生产 LLM 或生产用户数据。
- 自动化测试负责稳定回归，真机测试负责小程序能力、触摸、性能和视觉。
- 测试失败必须保留证据，不得通过放宽断言掩盖缺陷。

## 2. 测试分层

| 层级       | 范围                               | 工具/环境               | 执行时机     |
| ---------- | ---------------------------------- | ----------------------- | ------------ |
| 单元测试   | 规则、校验、坐标、格式化、状态机   | Vitest                  | 每次变更     |
| 组件测试   | 页面组件、表单、加载/错误态、交互  | React Testing Library   | 每次前端变更 |
| 契约测试   | DTO、错误码、响应信封、适配器      | Vitest + 固定夹具       | 每次接口变更 |
| 云函数集成 | 鉴权、数据库、事务、存储           | 隔离测试环境            | 每次后端变更 |
| 端到端     | 登录到保存搭配主链路               | 小程序自动化 + 测试环境 | 里程碑       |
| 真机与视觉 | 权限、键盘、安全区、手势、截图对比 | 微信开发者工具和真机    | 页面完成、RC |
| 非功能     | 性能、弱网、安全、隐私、兼容       | 测试环境与真机          | 里程碑、RC   |

## 3. 测试目录与命名

```text
src/**/__tests__/*.test.ts(x)      # 与模块共置的单元/组件测试
tests/contract/*.contract.test.ts  # 前后端契约
tests/integration/*.test.ts        # 云函数集成
tests/e2e/*.spec.ts                # 端到端
tests/fixtures/                    # 脱敏固定数据
tests/visual/baselines/            # 经确认的视觉基线
```

测试名称使用“条件 + 行为 + 结果”，例如：

```text
未勾选协议时提交微信登录，应阻止请求并提示用户
LLM 超过 4 秒未返回时，应使用本地规则并标记来源
删除被搭配引用的衣物并确认后，应同步移除所有搭配节点
```

## 4. 测试数据

- 固定北京时间和 `Asia/Shanghai`，避免测试随运行日期漂移。
- 使用确定的城市级天气夹具：极冷、微凉、舒适、炎热、昼夜温差大。
- 用户、位置和图片全部使用虚构或生成数据。
- 每个测试独立创建并清理数据；不得依赖执行顺序。
- 外部服务通过契约一致的 Fake/Stub 替代，必须保留成功、超时、限流和无效响应夹具。

## 5. 必测矩阵

### 5.1 登录

- 未登录首次打开直接进入首页，四项一级导航可访问，右上角显示默认头像。
- 默认头像及各受保护操作打开统一登录弹层，不跳转独立登录首屏。

- 协议未勾选、勾选、协议链接打开。
- 微信登录成功、code 失效、网络失败、会话过期和恢复。
- 不同操作的登录说明正确；“暂不登录”、登录失败和关闭弹层均保留当前表单、画板及待提交选择。
- 登录成功返回原页面并只自动续接一次原操作；重复回调、页面重载和会话恢复不重复写入。
- 新账号登录后直接续接原操作，不出现强制资料确认；默认昵称符合“微信用户 + 5 位数字”，默认头像可正常展示。旧占位昵称“微信用户”在下次登录时升级一次，已有自定义资料不被覆盖。
- 用户中心点击头像先显示“查看头像 / 修改头像”；查看可预览，修改后显示“使用微信头像 / 从相册选择 / 拍照上传”，三种来源均经过预览确认、头像上传、资料保存、失败重试和会话缓存同步。
- 点击昵称打开编辑弹层；微信原生昵称输入、手动输入、取消、空值校验、保存失败和成功同步均通过。后续登录不得覆盖用户手动修改的头像和昵称。
- 退出登录后敏感缓存清理并返回首页默认头像状态，云端数据和定位权限不被删除。

### 5.2 定位与天气

- 游客未定位时显示通用天气占位、城市胶囊“开启定位后查看当地天气”和“开启定位”按钮。
- 首次打开不自动请求定位；未登录点击“开启定位”可完成授权和天气加载，不触发登录。
- 拒绝授权后使用微信原生设置入口；在设置中同意定位后返回页面，应自动重新定位并直接显示当地天气。系统定位关闭、网络或城市解析失败后保留通用占位和重试入口。退出登录只清理会话，城市缓存与定位权限保持不变。

- 首次授权、拒绝、系统定位关闭、再次授权。
- 只展示城市，不出现手动切换。
- 天气首次加载、刷新中、成功、超时、缓存和无缓存失败。
- 拒绝或失败后继续显示通用占位和再次开启入口。
- 日期跨天、最高/最低温异常、缺少可选字段。

### 5.3 场景与行程

- 游客显示办公室、家、地铁通勤、商场四个示例场景，可打开新增和编辑表单但提交、修改和删除需登录。
- 登录用户的预设与自定义场景均可完整 CRUD。
- 体感枚举只能为偏冷、舒适、闷热、微凉。
- 行程只能选择已有场景，只管理当天。
- 游客首页显示地铁通勤和办公室两条示例行程；新增和编辑可填写，最终提交及删除需登录。
- 首次登录初始化示例场景和两条示例行程；初始化重试不重复，用户删除后再次登录不回填。
- 时间排序、跨午夜边界、停留时长边界、重复提交和并发冲突。
- 删除场景或行程的确认、取消和引用一致性。

### 5.4 建议

- 游客在未定位和已定位两种状态均可生成建议；未定位结果标明“通用穿衣策略”，游客体感固定为“舒适”。
- 游客生成和重新生成不要求登录，且不写入个人历史。

- 无行程、单行程、多行程和明显温差。
- 输入包含完整当天行程，不包含具体衣橱。
- LLM 正常、超时、错误、无效 JSON 和字段缺失。
- 规则降级内容、来源标识、重复点击禁用和缓存失效。
- 结果不包含不允许的具体衣物自动选择结论。
- 建议成功后展示“有帮助 / 一般 / 没帮助”；游客点击后登录，成功续接原评价，取消不产生记录。游客建议快照必须只能被同一可信微信身份在登录后续接，任意或他人建议 ID 继续返回失效。

### 5.5 衣橱与抠图

- 游客首次显示三件稳定示例衣物，示例与用户数据隔离。
- 游客可打开录入弹层；点击拍照或相册时触发登录，成功后自动继续原入口，取消后保留弹层。
- 游客可进入示例衣物编辑；保存修改和删除触发登录。

- 搜索、分类和颜色组合筛选。
- 相机/相册拒绝、文件过大、格式错误、上传失败。
- 抠图处理中、成功、失败、重试、取消和超时。
- 前后对比、重新拍摄、确认入库。
- 删除未引用/已引用衣物，确认后搭配节点同步移除。

### 5.6 自由试穿

- 游客使用三件示例衣物完成添加、移动、缩放、层级、移除和重置，均不要求登录。
- 游客可填写保存搭配表单；最终确认保存触发登录并在成功后续接一次。
- 游客“我的搭配”显示一个仅引用三件示例衣物的默认搭配；筛选和载入不登录，删除需登录。

- 空画布、加入多件衣物、选中、移动、缩放、层级和删除。
- 单指与双指手势冲突、快速连续操作、极端缩放和越界。
- 不同屏幕尺寸保存与恢复，坐标和层级容差。
- 搭配名称为空、保存失败、重复提交、筛选、载入和删除。
- 不出现固定人体或上身/下身/脚模板。

### 5.7 用户中心

- 游客点击默认头像只出现登录弹层，不展示虚拟用户中心。

- 资料和体感偏好读取/更新；页面不出现独立“编辑资料”入口，头像和昵称本身是编辑触发点。
- 定位权限状态变化、帮助和协议入口。
- 退出登录二次确认、取消和完成，并验证只清理本机会话。
- 重新登录后云端业务数据仍保留；MVP 不出现数据重置或账号注销入口。

## 6. 接口与安全测试

- 公开游客建议接口只接受版本化示例上下文和必要天气字段，不接受客户端伪造所有者或写入用户数据。
- 所有个人数据写接口及评价接口继续要求可信登录身份；覆盖未登录、伪造 `userId`、越权读取/修改其他用户数据。
- 场景和行程示例初始化幂等、可重试、按完整 `openId + appId` 身份隔离；重试只补齐缺失副本，不覆盖已有修改，不复活已删除副本。衣物和搭配不进行账号初始化。
- 缺字段、错误类型、超长文本、非法枚举和恶意文件。
- 幂等键重复提交、并发版本冲突、事务回滚。
- 响应信封、HTTP 状态、业务错误码与 `retryable` 一致。
- 日志不含密钥、Token、精确位置和私有图片 URL。
- 上传文件扩展名、MIME、大小、路径和访问权限校验。

## 7. 性能与兼容标准

- 建议生成目标小于 5 秒；记录 P50/P95 和降级率。
- 抠图目标小于 10 秒/张；记录成功率和重试率。
- 画布拖动与缩放无明显卡顿，连续操作不丢状态。
- 首屏、分包和包体积满足微信小程序当前发布限制；以发布前官方工具报告为准。
- 覆盖至少一台 iOS 和一台 Android 真机，以及项目确认的最低微信基础库版本。
- 覆盖窄屏、常规屏、刘海/安全区、键盘弹出和字体放大。
- 开发者工具测试至少覆盖编译、预览、真机调试、网络面板、存储、云函数日志、质量面板和上传前检查。
- 开启真实域名校验完成体验版回归；不得以“开发环境不校验合法域名”的结果代替。

## 8. 缺陷报告

每个缺陷必须包含：

```text
标题：
严重程度：阻断 / 严重 / 一般 / 轻微
环境与版本：
前置条件：
复现步骤：
期望结果：
实际结果：
复现率：
截图/录屏/日志：
关联需求与任务：
```

阻断：主链路无法完成、数据丢失、越权或无法构建。

严重：核心功能错误、无降级、明显隐私风险。

一般：有替代路径的功能或交互错误。

轻微：不影响任务完成的视觉或文案问题。

## 9. 发布门禁

发布候选必须满足：

- 所有 P0/P1 场景通过，无阻断和严重未解决缺陷。
- 单元、组件、契约、集成和主链路 E2E 全部通过。
- 真机矩阵通过，页面截图与确认原型无未解释偏差。
- 体验版在非开发者微信账号下完成主链路回归。
- 性能、安全、隐私、弱网和包体检查通过。
- 微信公众平台的服务类目、用户隐私保护指引、用户协议、隐私政策和审核资料已由项目所有者确认。
- 数据迁移/清理、监控和回滚方案已验证。
- `npm run lint`、测试、生产构建及 `git diff --check` 通过。
- 用户明确授权后才能部署、提审和发布。

## 10. 指标测试

- 固定夹具覆盖总用户排除测试账号、按用户去重、0 分母、UTC 时间和 `Asia/Shanghai` 日期边界。
- AI 建议功能复用率覆盖首次成功、不满 7 天不进分母、窗口内再次成功、窗口外再次成功和幂等重放。
- AI 建议用户使用率覆盖同一用户多条建议只计一个用户，规则降级建议同样计为成功使用。
- 场景填写完成率只统计有效修改并保存成功，覆盖预设与自定义场景；打开弹层、无变化提交和保存失败不计入。
- 行程设置完成率和个人体感设置修改率只统计服务端成功；默认体感不计入修改率。
- M10-01 增加三种评价、同用户同建议去重、评价修改、未评价、保存失败及三项评价指标测试。
- Markdown/CSV 报告必须展示统计截止时间、口径版本、百分比、分子和分母；0 分母显示 `—`。
