# 今天穿什么｜微信小程序开发与发布流程

> 本文说明从账号准备到正式发布的完整流程，并区分 Agent 可执行工作与必须由项目所有者完成的平台操作。产品与技术规则分别见 [PRODUCT.md](./PRODUCT.md)、[DEVELOPMENT.md](./DEVELOPMENT.md)、[ARCHITECTURE.md](./ARCHITECTURE.md)、[TASK.md](./TASK.md) 和 [TEST.md](./TEST.md)。

## 1. 基本原则

- 生产目标是微信小程序；当前 React/Vite 原型只作为 UI、交互与业务规则参照。
- 源码由 Taro 构建，微信开发者工具导入编译后的微信小程序目录，不直接导入浏览器原型。
- 微信公众平台和微信开发者工具中的账号、扫码、主体、成员、类目、隐私、提审和发布操作由项目所有者负责。
- Agent 可以编写代码、运行构建、准备配置、生成上传包和测试报告；涉及登录、外部写入、上传、部署、提审或发布时必须再次获得明确授权。
- 开发者工具中“跳过域名校验”只能用于本地排障，不能作为测试通过或发布依据。

## 2. 项目所有者需要准备的账号与资料

正式开发前应准备：

1. 已注册并完成主体认证的微信小程序账号。
2. 小程序名称、头像、简介和符合功能的服务类目。
3. 小程序 `AppID`。`AppSecret` 只允许保存在服务端密钥环境，不发给 Agent、不写入仓库。
4. 将实际开发者加入微信公众平台的项目成员，并按需要添加体验成员。
5. 安装微信开发者工具稳定版，并使用管理员或开发者微信扫码登录。
6. 若采用 CloudBase，在正确主体和地域下创建开发、测试和生产环境。
7. 用户协议、隐私政策、联系方式，以及微信公众平台“用户隐私保护指引”所需说明。
8. 天气、短信、LLM 和抠图供应商账号、资质、配额和服务端密钥。
9. 若小程序直接访问自建服务，准备已备案、HTTPS 有效且可配置为微信服务器域名的域名。

## 3. 环境分层

| 环境 | 用途 | 微信侧形态 | 数据要求 |
|---|---|---|---|
| 本地开发 | 组件和接口联调 | 开发者工具模拟器、预览、真机调试 | 只使用测试数据 |
| 测试 | 集成、体验版和回归 | 测试 CloudBase 环境、体验版 | 与生产隔离，可重复清理 |
| 生产 | 审核和正式用户 | 生产 CloudBase 环境、正式版 | 最小权限、备份和审计 |

推荐环境变量：

```text
development -> CloudBase 开发环境
test        -> CloudBase 测试环境
production  -> CloudBase 生产环境
```

不得让测试构建连接生产数据库、存储或付费短信通道。

## 4. 微信开发者工具接入

### 4.1 安装与项目导入

项目所有者完成微信开发者工具安装和扫码登录。Agent 创建 Taro 工程后，构建微信目标：

```powershell
npm run build:weapp
```

开发者工具应导入 Taro 的微信编译输出目录，通常为：

```text
miniprogram/dist
```

最终目录以 Taro 配置中的 `outputRoot` 为准。项目应提供：

```text
project.config.json
project.private.config.json   # 仅本机，不提交
sitemap.json
```

- `project.config.json` 保存可共享项目设置，不包含密钥。
- `project.private.config.json` 保存个人开发者工具设置，并加入 `.gitignore`。
- `AppID` 可以写入小程序项目配置，但 `AppSecret` 永远不能出现。

### 4.2 开发者工具日常用途

- 编译：检查页面、分包、基础库和控制台错误。
- 预览：生成二维码，在真实微信环境验证。
- 真机调试：检查权限、网络、存储、相机、相册、定位和手势。
- Audits/质量面板：检查性能、体验和常见规范问题。
- 上传：将通过门禁的构建上传为微信后台开发版本。
- 云开发控制台：管理环境、云函数、数据库、存储、日志和权限。

### 4.3 CLI 自动化

微信开发者工具提供命令行能力。使用前由项目所有者：

1. 在开发者工具中完成扫码登录。
2. 在设置中开启 CLI/服务端口能力。
3. 告知 Agent 本机开发者工具安装路径。

Windows 常见入口形式如下，实际路径以本机安装位置为准：

```powershell
& "<微信开发者工具安装目录>\cli.bat" --help
```

可自动化的动作包括打开项目、预览、自动化测试和上传。CLI 登录态属于本机敏感状态，不复制、不提交。Agent 每次执行预览、上传或部署前仍需获得用户授权。

## 5. 网络与域名

微信小程序只能与已配置的通讯域名通信。若存在客户端直连服务，应在微信公众平台“开发管理/开发设置”中配置对应类型：

- `request` 合法域名：HTTPS API。
- `uploadFile` 合法域名：上传服务。
- `downloadFile` 合法域名：图片和文件下载。
- `socket` 合法域名：仅在确有 WebSocket 时配置。

要求：

- 使用有效 HTTPS 证书和标准端口。
- 不使用 IP、`localhost`、临时隧道或未备案域名作为生产地址。
- 开发、测试、生产域名和数据环境明确隔离。
- 若全部客户端请求通过 CloudBase 小程序 SDK，仍需在真机和体验版验证权限及环境配置。
- 外部天气、LLM、短信和抠图由云函数服务端调用，供应商密钥不得下发到小程序。

## 6. 隐私、权限与平台配置

本项目涉及或可能涉及：

- 微信身份、昵称和头像。
- 手机号与短信验证码。
- 城市级定位。
- 相机、相册或用户选中的照片。
- 衣物图片、搭配数据和操作日志。

开发前由项目所有者确认实际收集项，并在微信公众平台配置“用户隐私保护指引”。代码必须做到：

- 仅在用户触发相关功能时申请权限。
- 申请前说明用途；拒绝后提供可理解的替代路径或设置引导。
- 收集项、用途、保存期限、第三方共享与删除方式和平台声明一致。
- 隐私声明未覆盖的数据不得收集或上传。
- 新增私密接口或第三方 SDK 时，先更新隐私清单并由项目所有者确认。

相机、相册、定位、手机号和用户资料必须在体验版真机验证；模拟器成功不代表合规或可发布。

## 7. 完整开发流程与责任分工

| 阶段 | Agent 负责 | 项目所有者负责 | 阶段出口 |
|---|---|---|---|
| 0. 账号准备 | 给出资料清单，检查配置缺口 | 注册/认证小程序，提供 AppID，配置成员和类目 | 开发者可登录并导入空项目 |
| 1. 工程初始化 | 建立 Taro 工程、构建脚本、项目配置和质量工具 | 在开发者工具扫码登录，确认 AppID 和基础库策略 | 开发者工具可编译基础页面 |
| 2. 云环境准备 | 建立云函数、数据库结构、权限规则和环境模板 | 创建 CloudBase 环境，确认地域、配额和计费 | 开发/测试环境联通且隔离 |
| 3. 功能开发 | 按 `TASK.md` 测试先行开发并更新进度 | 对产品决策、付费服务和视觉偏差做确认 | 单任务验收通过 |
| 4. 本地与真机联调 | 修复构建、接口、权限和兼容问题 | 使用真实微信扫码，配合授权/拒绝测试 | 核心链路真机通过 |
| 5. 外部服务接入 | 实现适配器、超时、降级和脱敏日志 | 开通供应商、配置资质/模板/配额，保管密钥 | 测试环境真实服务通过 |
| 6. 体验版 | 生成 RC、上传前报告，获授权后执行上传 | 确认上传版本，设置体验版与体验成员 | 体验版回归通过 |
| 7. 合规与提审准备 | 检查代码、隐私调用、包体、截图和说明 | 配置隐私指引、类目、基础信息、审核资料 | 发布门禁全部通过 |
| 8. 提交审核 | 准备版本说明和审核操作路径 | 在微信公众平台确认并提交审核 | 获得审核结果 |
| 9. 审核整改 | 根据审核意见定位并修复 | 提供审核通知和必要业务说明 | 审核通过 |
| 10. 发布 | 准备生产构建、监控和回滚清单 | 明确授权并点击发布/灰度相关操作 | 正式版可访问 |
| 11. 上线观察 | 监控错误、性能和外部服务 | 观察用户反馈、费用和平台通知 | 稳定观察期结束 |

## 8. 必须由项目所有者介入的检查点

### Gate A：工程开始前

- 提供 `AppID`。
- 确认小程序主体、名称和服务类目。
- 添加开发成员。
- 安装开发者工具并扫码登录。
- 确认是否使用 CloudBase，以及开发/测试/生产环境地域。

### Gate B：真实能力接入前

- 选择并开通天气、短信、LLM 和抠图服务。
- 确认费用上限、配额和数据处理区域。
- 在服务端环境中配置密钥，不通过聊天发送明文密钥。
- 配置短信签名和模板等需要主体审核的资源。

### Gate C：真机和体验版前

- 配合相机、相册、定位、微信登录和手机号流程测试。
- 在公众平台配置合法域名、体验成员和必要接口权限。
- 授权 Agent 使用开发者工具 CLI 预览或上传，或由你手动完成。

### Gate D：提审前

- 审阅用户协议、隐私政策和用户隐私保护指引。
- 确认服务类目、基础信息、版本说明和审核测试路径。
- 确认生产环境、付费额度、监控告警和客服/联系信息。

### Gate E：发布前

- 查看完整测试与风险报告。
- 决定是否发布以及发布时间。
- 明确授权生产部署和正式发布。
- 发布后关注微信平台通知、审核规则变化、用户反馈和费用。

## 9. 上传、提审与发布门禁

上传开发版本前：

- 工作区干净，版本号和提交可追踪。
- 单元、组件、契约、集成测试通过。
- `npm run build:weapp` 成功。
- 开发者工具编译无未解释错误。

设为体验版前：

- iOS 和 Android 真机主链路通过。
- 开发环境标识、调试开关和测试账号不进入体验构建。
- 合法域名、CloudBase 环境和隐私授权按真实配置验证。

提交审核前：

- `TEST.md` 发布门禁通过。
- 小程序基础信息、类目、隐私指引、用户协议和隐私政策完整。
- 审核人员可以按说明完成登录和核心体验；不得要求审核人员拥有内部账号。
- 版本说明准确描述本次能力，不承诺未实现功能。

正式发布前：

- 审核已通过。
- 生产云函数、数据库规则、存储权限、监控和配额已核验。
- 已准备回滚或紧急修复方案。
- 获得项目所有者本次明确授权。

## 10. 当前待项目所有者确认

- 小程序账号是否已注册并完成主体认证。
- `AppID` 是否已获得，开发成员是否已添加。
- 微信开发者工具是否已安装，安装路径和 CLI 是否可用。
- CloudBase 是否已开通，环境 ID、地域和计费方式。
- 小程序服务类目是否覆盖天气建议、衣橱图片与 AI 建议功能。
- 隐私政策、用户协议和用户隐私保护指引由谁最终审核。
- 天气、短信、LLM 和抠图供应商的选择、资质和预算。
- 用于真机测试的 iOS、Android 设备和体验成员。

## 11. 官方参考

- [微信开发者工具](https://developers.weixin.qq.com/miniprogram/dev/devtools/devtools.html)
- [微信开发者工具命令行](https://developers.weixin.qq.com/miniprogram/dev/devtools/cli.html)
- [小程序网络能力与服务器域名](https://developers.weixin.qq.com/miniprogram/dev/framework/ability/network.html)
- [小程序隐私授权](https://developers.weixin.qq.com/miniprogram/dev/framework/user-privacy/PrivacyAuthorize.html)
- [Taro React 开发说明](https://docs.taro.zone/docs/react-overall)
- [CloudBase 云函数调用云资源](https://docs.cloudbase.net/cloud-function/resource-integration/cloudbase)
