# 穿衣有数｜项目进度

> 稳定产品事实见 [PRODUCT.md](./PRODUCT.md)，最终 UI 规范见 [DESIGN.md](./DESIGN.md)，协作规则见 [AGENTS.md](./AGENTS.md)。

最后更新：2026-07-26

## 1. 当前阶段

阶段：**MVP 产品原型设计完成，进入开发阶段**。

当前状态：**M2-01 微信定位与城市识别已完成**。微信定位用途说明、首次授权、拒绝与设置页重新开启、系统定位关闭/失败重试、只读城市展示、城市级缓存恢复和服务端城市解析均已通过项目所有者真机验收；普通定位失败与真机超时统一使用通用失败文案，定位请求期间禁止重复点击。精确经纬度不持久化、不写日志。真实城市解析使用和风天气 GeoAPI，凭据仅配置在唯一环境 `ootd-ai-dev` 的云函数环境变量中。

已完成：**M2-02 天气适配器、缓存与首页天气卡**。和风天气实时/当日预报、单请求 2 秒超时与最多一次重试、CloudBase `weatherCache` 15 分钟城市级缓存、过期缓存降级、天气卡首次加载/主动刷新/刷新防重/失败重试及辅助字段缺失兼容均已通过真机验收。

已完成：**M3-01 场景查询与预设保护**。严格场景契约、办公室/家/地铁通勤/商场四个服务端稳定预设、按可信微信身份隔离的 CloudBase 自定义场景查询和 `GET /v1/scenes` 路由均已接入；公开响应不包含 `userId`。小程序已接入固定四项一级导航和登录态保护，场景库覆盖加载、空态、错误重试与预设保护，衣橱和试穿暂为后续里程碑占位页。唯一环境 `ootd-ai-dev` 已配置 `scenes` 集合、仅云函数可访问的数据权限和非唯一组合索引 `user_preset_updated`，项目所有者确认页面成果符合预期。契约 18 项、云函数 30 项、小程序 48 项测试以及类型、lint、格式和生产构建均通过。

下一任务：**M3-02 自定义场景新增与编辑（未开始）**。

已完成：**M3-02 自定义场景新增与编辑**。`POST /v1/scenes` 和 `PATCH /v1/scenes/:id` 两个新路由已接入，包含严格输入校验、预设保护、乐观锁版本冲突检测。前端已完成 `SceneForm` 组件（名称、类型下拉、温度、体感选择、备注）和 `SceneList` 新增/编辑集成。契约 18 项、云函数 37 项、小程序 48 项测试以及类型检查、lint、`build:cloudfunctions`、`build:weapp` 均通过。

下一任务：**M3-03 场景删除与引用处理（未开始）**。

开发者工具项目入口已调整为仓库根目录 `G:\24级王梓桐\OOTDapp`：根配置将 `miniprogram/dist/` 与 `cloudfunctions/` 同时纳入项目，解决单独导入 `miniprogram` 时无法显示 `api` 云函数的问题。

首次部署排障发现开发者工具在 TLS 断线后留下 Node.js 16.13 的失败函数占位，且原云函数构建产物位于 `dist/index.js`，不符合开发者工具固定的 `index.main` 入口。构建已修正为在函数根目录生成 Node.js 20.19 目标的 `index.js`；待网络稳定后删除失败占位并以 Node.js 20.19 重新部署。

首次登录联调确认 CloudBase 环境显示名称 `ootd-ai-dev` 与实际环境 ID 不同；小程序初始化及环境配置已统一改用完整 ID `ootd-ai-dev-d6g5hzex6925fcea7`，仍然只对应原有唯一 MVP 环境，不创建新环境。

第二次登录联调已将请求送达云函数，但日志仅有 `START` 且耗时、内存均为 0。根因验证为函数根入口曾被 `.gitignore` 排除，且 CommonJS 构建产物所在包错误声明为 ESM，导致云端无法解析 `index.main`。现已取消入口忽略并将函数包声明为 CommonJS，部署前通过本地入口加载冒烟检查。

当前 React/Vite 原型已被确认为 MVP 的 UI 与交互基线。旧 HTML 原型、Stitch 参考原型以及 `codex/ui-new-prototype` 工作树均不再作为需求或设计来源。

后续目标：在不改变当前产品边界和视觉方案的前提下，完成前端工程化、真实接口、数据持久化、权限流程和微信小程序生产实现。

开发入口文档：

- 飞书资料与 CLI：[FEISHU_DOCS.md](./FEISHU_DOCS.md)
- 开发规范：[DEVELOPMENT.md](./DEVELOPMENT.md)
- 技术方案：[ARCHITECTURE.md](./ARCHITECTURE.md)
- 开发任务与节奏：[TASK.md](./TASK.md)
- 测试规范：[TEST.md](./TEST.md)
- 微信小程序开发与发布流程：[WECHAT_MINIPROGRAM.md](./WECHAT_MINIPROGRAM.md)
- MVP 指标口径与开发指引：[METRICS.md](./METRICS.md)

## 2. 项目基线

- 仓库：<https://github.com/wangzitongwwwinner/OOTD>
- 默认分支：`main`
- 原型入口：`index.html`、`src/main.tsx`、`src/App.tsx`
- 页面组件：`src/components/`
- 原型服务：`server.ts`
- 视觉规范：[DESIGN.md](./DESIGN.md)
- 产品事实：[PRODUCT.md](./PRODUCT.md)

本地运行：

```powershell
npm install
npm run dev
```

访问 <http://127.0.0.1:3000>。

验证：

```powershell
npm run lint
npm run build
git diff --check
```

## 3. 已完成里程碑

### 3.1 产品范围确认

- 核心价值确定为结合实时天气、当天完整行程和场景温度的全天穿衣建议。
- 一级导航确定为首页、场景库、衣橱、试穿。
- 城市仅由微信定位自动识别，不提供手动切换。
- 场景体感确定为偏冷、舒适、闷热、微凉。
- 当天行程、场景库、衣橱和自由试穿构成 MVP 主链路。
- MVP 仅保留微信一键登录，不提供手机号登录/注册入口。

### 3.2 UI 与交互原型

- 登录页、首页、场景库、衣橱、试穿/我的搭配、用户中心已形成统一交互原型。
- 视觉方向确定为 Urban Wardrobe Narrative / Modern Editorial Minimalism。
- 已固化温暖中性色、编辑式宋体标题、24px 页面边距、8px 间距节奏、柔和卡片和线性图标体系。
- 首页城市改为只读“城市名 + 微信定位”。
- 建议加载文案改为“综合全天行程，编排穿脱方案…”，不再暗示读取衣橱。
- 产品 Logo 已迁移为本地资源。

### 3.3 原型基线迁移

- 当前 React/Vite 原型迁移到仓库根目录。
- 旧 `app.mjs`、旧测试、旧样式、旧静态服务、旧衣橱素材和 Stitch 参考原型已从基线移除。
- AI Studio 通用 README、元数据和临时启动/日志文件不进入新基线。
- 项目说明、产品、设计、进度和 Agent 规范已按最终 MVP 更新。

### 3.4 开发准备文档

- 已建立飞书项目文档与 CLI 访问指引。
- 已建立开发规范、生产技术方案、开发任务节奏和测试规范。
- 已补充微信开发者工具、平台配置、体验版、审核、发布和项目所有者介入流程。
- 生产方案采用 Taro 4.2.0/React 小程序与 CloudBase 模块化单体方向；天气、LLM 和抠图供应商已确认。

### 3.5 Taro 工程与质量基线

- 小程序依赖与构建封装在独立的 `miniprogram/` 包中。
- 已配置 TypeScript 类型检查、ESLint、Prettier、Vitest、React Testing Library 与 jsdom 测试环境。
- 根项目提供小程序类型检查、Lint、测试和微信构建代理命令。

### 3.6 CloudBase 与共享契约基线

- `packages/contracts` 提供统一响应信封、稳定错误码、MVP 数据类型和严格运行时校验。
- `cloudfunctions/api` 使用 Nodejs20.19 和 `@cloudbase/node-sdk`，只从平台上下文获取可信微信身份。
- `cloudbase/` 声明九个集合、关键索引和默认拒绝客户端数据库直读写的规则；写入云端前必须再次获得授权。

### 3.7 小程序 UI 基础层

- `miniprogram/src/styles/tokens.scss` 集中维护确认的品牌色、本地字体回退、8px 间距节奏、圆角和浮层阴影，不依赖远程字体。
- 已建立 Button、Card、BottomSheet、FormField、EmptyState、ErrorState 六个无业务基础组件。
- Vitest 组件测试使用等价 HTML 元素模拟 Taro 平台组件，真实微信组件行为仍由生产构建、开发者工具和真机验证负责。
- M0-04 自动化结果：7 个测试文件、12 个测试通过；TypeScript、ESLint、Prettier 与 `build:weapp` 通过。
- 2026-07-17 项目所有者完成开发者工具视觉验收，确认窄屏、宽屏、加载禁用和底部弹层交互均无问题。

### 3.8 自动化质量基线

- `tests/fixtures/` 提供固定 `Asia/Shanghai` 时间、虚构用户、城市级天气和外部服务 Stub 结果，不包含真实身份、精确定位或图片。
- `config/environments.json` 将 development、test、production 全部映射到上海地域 `ootd-ai-dev`，自动清理必须检查 `__test__` 标记。
- `.env.example` 只保留公开环境信息和空的服务端密钥变量；真实值继续由本地环境或密钥管理提供。
- `.github/workflows/quality.yml` 使用 Node.js 20.19.2、四个锁文件和仓库只读权限，仅运行本地 `npm run quality`，不访问 CloudBase、微信工具或供应商服务。
- 本地结果：根测试 17/17、小程序 12/12、契约 3/3、云函数 4/4；全部类型检查、Prettier、Web/Taro/云函数构建通过。
- 提交 `db05172` 的首次 GitHub Actions Quality 工作流通过，运行编号 `29630771382`，耗时 1 分 12 秒；GitHub 提示 `actions/checkout@v4`、`actions/setup-node@v4` 的内部 Node 20 运行时已由平台强制迁移到 Node 24，项目配置的 Node.js 20.19.2 测试环境仍正常生效。

## 4. 当前原型能力

- 微信一键登录原型。
- 只读定位城市及天气展示、刷新。
- 基于全部当天行程生成穿衣建议，支持 Gemini 可选调用与本地规则降级。
- 场景新增、编辑和删除。
- 当天行程新增、编辑和删除。
- 衣橱搜索、筛选、上传、抠图确认演示、编辑和删除。
- 自由试穿的添加、拖动、缩放、层级、删除、保存和重新载入。
- 用户体感偏好、定位授权、帮助、重置和退出原型。
- 浏览器本地存储保存主要原型状态。

## 5. 已确认延期项

以下内容不进入当前 MVP，后续迭代再评估：

- 未来 7 天行程编辑和历史行程只读。
- “穿对了 / 穿多了 / 穿少了”反馈及快照记录。
- 从行程流程新建场景并自动同步场景库。
- 被引用衣物软删除和历史搭配占位保护。
- AI 具体衣橱挑选、搭配对比、衣橱利用率和购买建议。
- OOTD 日历、每日照片、分享卡片、社区和电商。

## 6. 开发阶段优先级

### P0：工程与真实数据

1. 明确 React 原型到微信小程序生产代码的迁移策略。
2. 建立开发、测试、环境变量和持续集成基线。
3. 设计用户、场景、当天行程、衣物和搭配的数据持久化。
4. 接入微信登录和微信定位。
5. 接入天气服务并实现超时、刷新和降级。

### P1：AI 与衣橱服务

1. 固化建议接口输入输出结构。
2. 限制提示词和本地规则只输出衣物类型、材质和薄厚，清理版型/风格词。
3. 接入适合中国大陆环境的 LLM 服务和本地规则降级。
4. 接入相机/相册、抠图、确认和重拍流程。

### P2：试穿与质量

1. 完成画板节点持久化、恢复和边界处理。
2. 建立关键状态单元测试、组件测试和移动端视觉回归。
3. 完成权限拒绝、服务超时、隐私与数据安全验证。
4. 准备建议质量评估集和真机测试。

## 7. 当前风险

- 整个 MVP 共用 `ootd-ai-dev`，没有环境级开发、测试、生产隔离；测试数据、调试配置、权限和发布数据必须通过命名、标记、清理清单及发布门禁做逻辑隔离，误操作影响正式数据的风险高于多环境方案。
- Taro 4.2.0 的本地构建、开发者工具编译、组件按需注入代码质量扫描、预览和真机打开均已通过，基础库固定为 3.16.2。
- Taro 4.2.0 的小程序生产依赖审计仍报告 12 个上游漏洞（3 个低危、6 个中危、3 个严重），涉及 `esbuild`、`swiper`、`uuid` 和 `webpack`；其中部分暂无上游修复，开发服务不得暴露到不可信网络，并在版本更新时复查。
- `@cloudbase/node-sdk@3.18.3` 的生产依赖审计报告 5 个上游漏洞（1 个中危、4 个高危），来自其旧版 Axios 与 lodash 依赖；当前无不降级 SDK 的直接修复，真实外部请求接入前必须复查并限制请求目标、输入规模和出网范围。
- Taro 4.2.0 小程序依赖审计报告 12 个上游漏洞（3 个低危、6 个中危、3 个严重），主要位于构建工具链及 Taro 间接依赖的 `swiper`；当前版本没有完整兼容修复。M1-01 未使用 Swiper，也不开放开发服务器到不可信网络，后续升级 Taro 前需先跑完整质量门禁与开发者工具回归。
- 当前服务包含模拟天气和原型 AI 接口，不能视为生产服务。
- Gemini 与 Google 相关调用不一定适合最终中国大陆生产环境，需要开发阶段替换或确认。
- 当前浏览器 `localStorage` 仅用于原型演示，不能作为生产数据方案。
- 微信权限、登录、定位、相机和相册尚未进行真机验证。
- AI 提示词和本地温度规则仍可能出现版型/风格词，必须在 AI 开发阶段处理。

## 8. 决策记录

| 日期       | 决策             | 说明                     |
| ---------- | ---------------- | ------------------------ |
| 2026-07-10 | 建议综合全天行程 | 不按单场景分别生成建议。 |

### 2026-07-22 M5 建议链路进展

- M5-02 已完成：TokenHub LLM 成功返回结构化建议，`source=llm`；Responses API 正确读取 `message/output_text`，并具备 4 秒超时与本地规则降级。
- M5-03 已完成并通过项目所有者验收：推荐请求仅接受城市级触发信息，服务端从可信微信身份读取体感偏好，按天气 `localDate` 读取完整当天行程，并组装 LLM/规则输入。
- M5-04 已完成并通过项目所有者验收：首页已接入主动生成/刷新建议、加载防重、结构化分层与提示、`AI 建议`/`通用策略` 来源标识以及失败重试。
- M6-01 已完成并通过项目所有者验收：已建立衣物类别与处理状态契约、可信微信身份隔离的 CloudBase 仓储边界和 `GET /v1/clothing` 列表路由；`clothing` 集合和云端空列表已验证。
- M6-02 已完成并通过项目所有者验收：已实现 JPG/PNG 与 10 MB 上限校验、服务端生成的用户隔离上传路径、衣物草稿创建、微信云存储原图上传及正式 `fileID` 回写；衣橱页已提供拍摄指南、相机/相册入口、上传防重、成功和失败状态，并在调用相机前显式处理 `scope.camera` 授权。相册由微信选择器完成用户授权，不存在独立的 `scope.album`。
- M6-03 已完成并通过项目所有者真实云端验收：腾讯云 MPS 已开通并完成 COS 服务角色授权，`image_processing_jobs`、云函数密钥与输出存储配置已就绪；真实衣物图片成功生成 `providerTaskId` 和 `processedFileId`，任务最终为 `succeeded`，无云函数错误。已实现用户隔离的抠图任务状态机、MPS `30031` 场景适配、轮询、前后对比、失败回落和最多一次重试；供应商提交异常不会让衣物永久停留在处理中。云函数密钥变量使用非保留名称 `MPS_SECRET_ID` 与 `MPS_SECRET_KEY`，小程序轮询使用微信运行时兼容的取消标记，不依赖 `AbortController`。
- M6-06 已完成并通过项目所有者云端验收：支持删除已确认衣物，通过 DELETE /v1/clothing/:id 乐观锁原子删除；衣橱卡片提供危险样式删除按钮，删除后列表即时移除。
- M6-05 已完成并通过项目所有者云端验收：支持修改衣物名称、类别和颜色，通过 PATCH /v1/clothing/:id 乐观锁更新仅已确认衣物；衣橱卡片提供编辑入口、Picker 类别切换和保存防重。：支持修改衣物名称、类别和颜色，通过 PATCH /v1/clothing/:id 乐观锁更新仅已确认衣物；衣橱卡片提供编辑入口、Picker 类别切换和保存防重。
- M6-04 已完成并通过项目所有者云端验收：抠图成功后进入 `review` 待确认状态，只有用户点击“确认入库”后才转为 `ready` 并出现在衣橱；衣橱页已接入已确认衣物列表、名称搜索以及类别和颜色组合筛选，并保留重新拍摄入口。确认接口校验可信微信身份和任务版本，列表仓储仅查询 `ready` 衣物。
- M7 自由试穿与搭配已开始：现有小程序自由画板 Spike 已接入衣橱衣物、单指移动、双指缩放、前置和移除。2026-07-26 完成归一化坐标模型的首轮自动化验证，统一按画布可移动范围保存与恢复坐标，修复跨尺寸恢复偏移，并集中处理 `0.3～3` 缩放边界和层级前置。当前仍需完成真机手势/性能 Spike、搭配服务端持久化、列表、载入和删除，尚未进入验收。
- 2026-07-26 M7 自由画布 Spike 已通过 Android 真机验收（微信 8.0.74）：留白画板、添加多件衣物、单指移动、双指缩放、前置层级、移除、边缘操作和连续操作性能均符合预期，控制台无错误。当前没有可用 iOS 设备，iOS 真机兼容性验收暂无法执行，不作为继续开发的阻塞条件；搭配保存后的跨尺寸恢复仍待后续验证。
- 2026-07-26 M7 搭配持久化开始实施：已新增严格共享契约，限定归一化坐标、`0.3～3` 缩放、画布版本、标签、公开字段和最大节点数量，拒绝客户端身份与越界数据；契约测试 34/34 及契约类型检查通过。服务端保存/列表/详情核心用例已完成可信身份隔离和衣物所有权校验，定向测试 4/4 通过；CloudBase 仓储、路由、小程序保存与重新载入尚未接入。
- 2026-07-26 M7 服务端搭配保存与查询已完成本地实现：`GET /v1/outfits`、`POST /v1/outfits` 和 `GET /v1/outfits/:id` 已接入路由，CloudBase `outfits` 仓储按可信身份解析用户并隔离数据，保存前验证节点衣物属于当前用户，公开响应移除 `userId` 与数据库内部字段。同步移除了图片处理分支中误嵌套的重复衣物更新路由，并清理云函数 `package.json` 重复测试脚本键。云函数测试 100/100 与生产构建通过；尚未部署云函数，小程序保存与重新载入仍待接入。
- 2026-07-26 M7 小程序保存与重新载入已完成本地最小闭环：新增搭配 API 服务层及严格响应校验，试穿页可填写名称保存当前归一化节点、保存期间防重复提交、展示“我的搭配”并载入节点恢复画板。新增服务层 4 项和管理组件 3 项测试；小程序全量测试 80/80、类型检查、格式检查及微信生产构建通过。云函数尚未部署，因此暂不能进行真实保存验收；季节/颜色标签录入、删除搭配和衣物删除联动仍待后续任务。
- 2026-07-26 M7 云函数部署未成功：项目所有者已授权仅部署现有 `api` 函数。部署前确认唯一环境 ID 为 `ootd-ai-dev-d6g5hzex6925fcea7` 且云端存在 `api`；CLI 全量部署在本地打包阶段因 `EISDIR` 失败，未写入云端。随后两次仅更新根入口 `index.js` 的官方增量部署均在获取微信云 API 签名头时返回平台系统错误 `41002`，仍未写入云端。需在开发者工具重新确认登录状态后手动增量上传，或待平台恢复后重试。
- 2026-07-26 项目所有者已通过微信开发者工具成功部署最新 `api` 云函数；为兼容开发者工具内置的旧版 TypeScript，云函数配置调整为 `target: ES2020`、`moduleResolution: Node`，本地 Node.js 20.19 生产构建通过且开发者工具不再报告配置错误。M7 真实搭配保存、列表和重新载入现已具备真机验收条件。
- 2026-07-26 M7 真机验收发现“我的搭配”仅以“载入 + 名称”的整块按钮呈现，记录虽已成功保存和返回，但缺少独立的搭配信息展示。现已改为名称、衣物数量和独立载入按钮组成的列表卡片，并增加回归测试；小程序全量测试 80/80、类型检查和微信生产构建通过，待重新编译后复验展示与载入。
- 2026-07-26 M7 搭配持久化已通过 Android 真机复验（微信 8.0.74）：保存、列表展示、重新进入后的数据保留、位置/缩放/层级恢复以及多条搭配隔离均符合预期，控制台与云函数日志无错误。当前剩余真机覆盖缺口为不同屏幕尺寸恢复和 iOS 兼容性；因暂无相应设备，不阻塞后续开发。
- 2026-07-26 被搭配引用衣物的删除保护已完成本地实现：删除契约新增引用移除确认标记和 `CLOTHING_REFERENCED` 稳定错误码；首次删除检测到引用时不写数据，小程序展示危险操作二次确认，确认后 CloudBase 在同一事务中移除多节点搭配中的对应节点、删除变空的单节点搭配，并删除衣物。契约测试 35/35、云函数测试 101/101、小程序测试 81/81、小程序类型检查及两端生产构建通过；根级 `npm run lint` 仍被既有云函数测试夹具缺少 `update/delete` 方法及既有 `INVALID_REQUEST` 错误码类型问题阻塞。本功能尚未部署，需部署最新 `api` 后真机验收。
- 2026-07-26 项目所有者已成功部署包含“被搭配引用衣物安全删除”的最新 `api` 云函数；功能进入 Android 真机验收。
- 2026-07-26 引用删除真机验收发现试穿页数据不同步：删除衣物后旧衣物仍可选择、搭配仍显示旧节点，新增衣物也不出现。根因是微信 Tab 页面切换不卸载，而试穿衣橱与搭配列表仅在首次挂载请求。现已改为每次试穿页显示时重新请求衣橱和搭配，并清除当前画板中已不存在的衣物节点；新增刷新回归测试，小程序全量测试 82/82、类型检查和微信生产构建通过。该修复仅涉及小程序前端，无需再次部署云函数，待重新编译后复验。
- 2026-07-26 项目所有者已通过 Android 真机复验试穿页数据同步：删除衣物及搭配节点同步、新增衣物同步均符合预期。继续实施 M7 剩余的已保存搭配删除能力。
- 2026-07-26 M7 已保存搭配删除进入本地实现：新增严格版本请求、可信身份仓储删除、`DELETE /v1/outfits/:id` 路由、小程序服务和列表确认删除操作。服务端删除用例 5/5、小程序服务用例 5/5、云函数构建和小程序类型检查通过；仍需补齐列表确认/取消组件测试、全量回归和微信生产构建后才能部署验收。
- 2026-07-26 M7 已保存搭配删除已完成本地质量门禁：列表提供删除按钮和危险操作确认，取消不请求删除，确认后按可信身份与期望版本删除并即时移出列表；NOT_FOUND、CONFLICT 和仓储异常均由服务端稳定映射。契约测试 35/35、云函数测试 102/102、小程序测试 84/84、小程序类型检查、两端生产构建及 `git diff --check` 均通过。尚未部署最新 `api`，需部署并重新编译小程序后真机验收。
- 2026-07-26 项目所有者已成功部署包含已保存搭配删除能力的最新 `api` 云函数；功能进入 Android 真机验收。
- 2026-07-26 已保存搭配删除通过 Android 真机验收，取消、确认删除、列表即时更新和持久状态均符合预期。M7 随后补齐搭配标签录入：保存区新增季节与颜色标签输入，兼容中英文逗号，自动去空白、去重并限制各 12 项；载入时回填，列表以低视觉权重胶囊展示。组件测试 6/6、小程序全量测试 85/85、类型检查、格式检查及微信生产构建通过；仅涉及前端，待重新编译后真机验收。
- 2026-07-26 搭配标签通过 Android 真机验收，M7 在现有设备条件下已完成；不同尺寸和 iOS 仍作为设备覆盖缺口。M8 用户中心开始实施：新增由首页入口进入的二级页面，展示昵称与最近体感，提供定位授权、帮助、协议/隐私和数据管理含义说明；退出登录使用危险样式与二次确认，明确只清本机会话且不删除云端数据，确认后清缓存并重新启动到首页登录态。组件测试 1/1、小程序全量测试 86/86、类型检查、格式检查和微信生产构建通过，待真机验收；云端数据清理尚未实现。
- 2026-07-26 M8 用户中心第一阶段通过 Android 真机验收：首页入口、资料摘要、说明入口、退出取消、确认后清理会话、重新进入保持未登录以及重新登录后云端数据保留均符合预期。下一步为云端数据清理，但现有产品文档未定义是否保留用户账号/偏好，也未定义衣物原图和抠图结果的文件删除范围；实施不可逆清理前需项目所有者确认边界。
- 2026-07-26 产品边界确认：MVP 暂不提供数据重置，只保留退出登录；账号注销后续按微信平台规则单独设计。已从用户中心移除“数据管理说明”入口，并同步 `PRODUCT.md`、`TASK.md` 和 `TEST.md`，避免把退出登录、数据重置和账号注销混为一谈。
- 2026-07-26 M8 按确认后的 MVP 边界完成：用户中心资料、体感摘要、定位授权、帮助、协议/隐私和退出登录均已通过 Android 真机验收；数据重置及账号注销不进入 MVP。边界调整后组件测试 1/1、小程序全量测试 86/86、类型检查、格式检查和微信生产构建通过。
- 2026-07-26 根据项目所有者要求，用户中心保留纯说明入口“账号与数据说明”，明确告知“退出登录不会删除云端数据。当前版本暂不支持账号注销；相关能力后续将按微信平台规则提供。”该入口不提供数据重置或注销操作。
- 2026-07-26 M9-04 开始实施：已增加排除测试用户、建议 ID 去重、7 日完整观察窗口和零分母处理的纯指标计算；Markdown/CSV 报告会展示截止时间、口径版本、分子分母和暂不可用的三项评价指标。场景编辑、行程保存、体感修改成功后记录用户首次成功时间，建议成功后按请求 ID 幂等保存 `recommendationSnapshots`；指标记录失败不会把已成功的业务操作误报为失败。云函数全量测试 102/102、指标夹具测试 4/4 通过，待云函数生产构建与部署后验收。
- 2026-07-26 M9-04 已通过 Android 真机与云端数据验收：三个首次成功标记、推荐快照字段、首次时间防覆盖、第二次推荐新增快照以及控制台和云函数日志均符合预期。M9-04 完成，进入 M9-05 发布候选预检；开发版本上传与体验版设置仍需项目所有者单独授权和操作。
- 2026-07-26 M9-05 发布候选预检完成：修复行程组件 Effect 同步状态更新的 Lint 阻塞、统一衣物参数错误为稳定 `VALIDATION_ERROR`，并补齐测试仓储接口。小程序测试 87/87、契约测试 35/35、云函数测试 102/102、仓库级类型门禁、ESLint、Prettier、小程序生产构建、云函数生产构建和差异检查均通过；微信包共 37 个文件、约 0.42 MiB。当前改动尚未提交，必须先获得提交授权形成可追踪版本，再单独获得微信开发版本上传授权。
- 2026-07-26 微信开发版本 `0.9.0` 已通过开发者工具 CLI 上传成功，AppID 为 `wxf9475a79c2296561`，平台上传包体为 414.3 KB。代码与文档按云函数/契约、小程序、项目文档三个批次提交并推送至 `codex/mvp-development`；M9-05 仍待项目所有者在微信公众平台设为体验版、配置体验成员并完成体验版回归。
- 2026-07-26 项目所有者确认当前 Taro 页面与既定 React 交互原型存在明显偏差，发布顺序调整为：React 原型逐页还原 → AI 穿衣建议评价卡片 → 新体验版回归 → 提交审核 → 正式发布。`0.9.0` 仅保留为开发快照，不设为审核候选。M10-01 解除暂缓，但必须等待原型还原视觉验收通过后启动；实施计划见 `docs/superpowers/plans/2026-07-26-prototype-parity-feedback-release.md`。
- 2026-07-27 项目所有者进一步确认：AI 穿衣建议评价卡片完成后，必须单独完成对应指标数据埋点、三项指标计算、Markdown/CSV 报告和真实数据核对，验收通过后才能准备上线。新增 M10-02 作为独立发布门禁，发布链路调整为：原型还原 → 评价卡片 → 评价指标埋点与报告 → 新体验版 → 审核 → 发布。
- 2026-07-27 M10-01 开始实施：项目所有者确认评价卡片 Demo，已实现“有帮助 / 一般 / 没帮助”三选一、提交中、成功、失败重试和修改评价；服务端按可信用户与稳定 recommendationId 校验并覆盖保存。机器人头像改用全新缓存键的纯黑线条 SVG。契约 37/37、云函数 104/104、小程序 105/105 测试通过，待云函数部署和 Android 真机验收。
- 2026-07-27 M10-01 验收通过：云函数已部署，`recommendationFeedbacks` 集合已建立，Android 真机评价保存与修改通过。后续页面继续按首页标准严格还原 React 原型，下一页为场景库。
- 2026-07-27 场景库 UI 还原完成：页面标题与说明、场景卡片比例、类别与温度图标、体感标签、编辑/删除操作、新增卡片，以及居中场景表单的输入、类型选择、`-10～40°C` 温度滑杆、体感选择和操作按钮均已按确认截图校准；下一页为衣橱，但按项目所有者要求提交后暂停开发。
  | 2026-07-10 | 试穿使用自由画板 | 不采用固定上身/下身/鞋履模板。 |
  | 2026-07-12 | 移除日历与分享卡片 | 不进入 MVP。 |
  | 2026-07-14 | 当前 React 原型成为唯一 UI 基线 | 旧根目录原型和独立工作树方案废弃。 |
  | 2026-07-14 | 视觉系统采用 Urban Wardrobe Narrative | 后续 UI 修改必须保持一致。 |
  | 2026-07-14 | 城市仅微信自动定位 | 原型使用只读定位状态，不提供下拉切换。 |
  | 2026-07-14 | 场景体感沿用原型枚举 | 偏冷、舒适、闷热、微凉。 |
  | 2026-07-14 | 简化 MVP 行程和反馈范围 | 未来/历史行程、体感反馈快照、行程内新建场景延期。 |
  | 2026-07-14 | MVP 沿用当前衣物删除方案 | 软删除与占位保护延期。 |
  | 2026-07-14 | 手机号登录/注册入口保留（已被后续决策取代） | 2026-07-16 改为 MVP 仅保留微信一键登录。 |
  | 2026-07-16 | 建立生产开发文档体系 | 新增飞书访问、开发、架构、任务和测试文档。 |
  | 2026-07-16 | 生产架构采用 React 迁移路线 | 以 Taro/React + CloudBase 为开发基线，外部供应商通过决策门后接入。 |
  | 2026-07-16 | MVP 仅使用一个 CloudBase 环境 | 整个 MVP 使用上海地域的 `ootd-ai-dev`，不创建测试或生产环境，接受缺少环境级隔离的风险。 |
  | 2026-07-16 | 产品更名为“穿衣有数” | 更新产品事实、项目文档、浏览器原型和微信小程序工程中的用户可见名称。 |
  | 2026-07-16 | 固定 M0-01 技术选型 | Taro 4.2.0；和风天气、腾讯混元、腾讯云媒体处理 MPS；整个 MVP 仍仅使用 `ootd-ai-dev`。 |
  | 2026-07-16 | 移除手机号登录入口 | MVP 仅提供微信一键登录，代码、任务和测试范围同步收敛。 |
  | 2026-07-22 | 固化 MVP 指标体系 | M9-04 建立近似指标数据与 Markdown/CSV 报告；“AI 建议生成完成率”改名为“AI 建议用户使用率”；“场景填写完成率”改为至少成功完成一次有效场景编辑。 |
  | 2026-07-26 | 调整发布前开发顺序 | 先完成 React 原型逐页还原，再实现 M10-01 AI 建议评价卡片，随后重新生成体验版并提交审核、发布；`0.9.0` 仅为开发快照。 |
  | 2026-07-27 | 评价指标成为独立上线门禁 | M10-01 完成评价交互与存储，M10-02 再完成三项指标埋点、计算、报告和数据核对；M10-02 验收前不得准备上线。 |

## 9. 交付规则

- 新开发不得重新引入废弃原型文件或旧产品范围。
- 每项开发完成后更新本文件中的真实状态，不把计划写成已完成。
- 产品或视觉决策变化先经用户确认，再更新主文档。
- 提交与推送前必须验证，并再次向用户确认。
