# M2-01 微信定位与城市识别实施计划

1. 先定义城市解析请求/响应契约，拒绝额外身份字段和越界经纬度。
2. 建立服务端城市解析用例与供应商适配器，设置超时和可读错误，不记录或存储精确坐标。
3. 建立小程序定位适配器与权限状态机，覆盖首次授权、拒绝、系统关闭、网络失败和重试。
4. 在首页展示只读“城市名 + 微信定位”，不提供手动切换。
5. 运行相关测试、Lint、类型检查、云函数与微信构建；配置真实环境变量后在开发者工具和真机验收。

约束：整个 MVP 只使用现有 `ootd-ai-dev`；和风天气 Host/Key 不进入仓库；未经确认不提交或推送。
