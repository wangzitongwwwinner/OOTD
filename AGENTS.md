# 今天穿什么｜Agent 全局工作规范

> 本文件适用于本仓库中的所有 Agent。当前产品原型设计已完成，后续工作以开发、测试和交付为主。

## 1. 启动协议

开始任务前按类型最小化读取：

1. 所有 Agent 必读本文件。
2. 产品、需求、业务规则：读取 [PRODUCT.md](./PRODUCT.md)。
3. UI、交互、视觉实现：读取 [PRODUCT.md](./PRODUCT.md) 和 [DESIGN.md](./DESIGN.md)。
4. 开发、测试、排障：读取 [PRODUCT.md](./PRODUCT.md) 相关章节和 [PROGRESS.md](./PROGRESS.md)；涉及 UI 时再读取 `DESIGN.md`。
5. 规划、状态汇报、接力：读取 [PROGRESS.md](./PROGRESS.md)。

不要无差别读取全部文档。先用 `rg` / `rg --files` 定位，再读取必要片段。

## 2. 信息源优先级

1. 用户当前对话中的最新明确指令。
2. [PRODUCT.md](./PRODUCT.md) 中固化的产品事实。
3. [DESIGN.md](./DESIGN.md) 中确认的视觉与交互规范。
4. [PROGRESS.md](./PROGRESS.md) 中的实施状态和决策记录。
5. 当前代码表现。
6. Agent 推测。

代码实现不等于产品事实。发现代码与高优先级文档不一致时，应报告并按授权范围处理，不得修改文档迎合错误实现。

## 3. 文档职责

- `PRODUCT.md`：MVP 产品边界、业务规则、数据对象和验收要求。
- `DESIGN.md`：最终原型视觉系统、页面结构和交互表现。
- `PROGRESS.md`：里程碑、当前状态、阻塞、决策和下一步。
- `AGENTS.md`：协作、开发、测试、Git 和安全规范。

同一事实只在一个主文档中完整定义，其他文档使用链接引用。

## 4. 当前代码基线

当前原型和开发基线位于仓库根目录：

- `index.html`：Vite 入口。
- `src/main.tsx`、`src/App.tsx`：React 应用入口和全局状态。
- `src/components/`：登录、首页、场景库、衣橱、试穿和用户中心组件。
- `src/assets/`：产品 Logo 与原型图片资源。
- `server.ts`：Express 开发服务与天气、建议原型接口。
- `src/index.css`：全局样式与 Tailwind 入口。
- `vite.config.ts`、`tsconfig.json`：构建与类型配置。

命令：

```powershell
npm install
npm run dev
npm run lint
npm run build
```

本地地址：<http://127.0.0.1:3000>。

## 5. Agent 角色边界

### 5.1 产品 Agent

- 维护需求边界、业务规则、验收标准和优先级。
- 产品决策确认后更新 `PRODUCT.md`，执行状态更新 `PROGRESS.md`。
- 不擅自决定视觉细节或技术实现。

### 5.2 UI/前端 Agent

- 严格依据 `PRODUCT.md` 与 `DESIGN.md` 实现。
- 当前原型是已确认的 MVP UI 基线；优先复用现有组件和视觉令牌，不因个人偏好重做页面。
- 未经确认不得改变信息架构、功能范围、页面顺序或文案含义。
- HTML/React 原型不等于最终微信小程序生产架构。

### 5.3 后端/AI Agent

- 依据产品对象设计接口、存储与降级策略。
- AI 建议综合当天完整行程，不按单场景分别生成，也不从具体衣橱自动挑选衣物。
- 天气、LLM、抠图服务必须有超时、错误和降级策略。
- 不在日志、前端或提交中暴露密钥、Token、隐私和精确定位。

### 5.4 测试 Agent

- 从产品验收标准生成测试，不只验证当前实现。
- 覆盖主链路、边界、权限拒绝、接口失败和状态恢复。
- 只诊断时不得顺带修改产品代码。
- 问题报告包含复现步骤、期望、实际结果和严重程度。

## 6. 当前 MVP 决策

- 一级导航固定为：首页、场景库、衣橱、试穿。
- 首页城市仅由微信定位识别，不提供手动切换。
- 首页行程只覆盖当天，且从已有场景中选择。
- 场景体感枚举为：偏冷、舒适、闷热、微凉。
- MVP 保留微信一键登录和手机号登录/注册入口。
- MVP 暂不实现未来 7 天/历史行程、体感反馈快照、从行程新建场景、被引用衣物软删除占位。
- 上述延期项进入后续迭代，不得在 MVP 开发中自行恢复。

## 7. 工作原则

- 分析、评审、诊断任务只做只读检查和报告。
- 修改或构建任务在授权范围内直接完成并验证。
- 改变产品方向、信息架构、外部服务或数据模型前先确认。
- 修改前运行 `git status --short`，保留用户和其他 Agent 的未提交工作。
- 每次只解决当前需求，不夹带无关重构。
- 不在未验证时宣称完成；失败时报告真实状态。
- 遇到网络错误或超时立即反馈，不长时间静默等待。

## 8. 开发与验证

行为变更遵循测试先行：

1. 写能证明期望行为的失败测试或最小可重复检查。
2. 确认因缺少功能而失败。
3. 写最小实现。
4. 运行 `npm run lint` 和 `npm run build`，并执行相关测试。
5. 运行 `git diff --check`。

纯视觉修改至少运行类型检查、构建和可用环境下的视觉检查。

## 9. Git 与提交

- 默认分支：`main`。
- 执行 `git commit` 或 `git push` 前必须再次向用户确认。
- 用户确认后，只暂存约定范围内的文件。
- 不提交 `node_modules`、`dist`、日志、截图、缓存和临时启动脚本。
- 提交信息使用简洁中文。
- 提交前运行验证和 `git diff --check`；推送后核验远端分支指向新提交。
- 不自动创建 PR、Tag、Release 或部署，除非用户明确要求。

## 10. 外部服务与安全

- `.env` 和真实密钥不得提交；仅保留 `.env.example`。
- 定位、相机和相册遵循最小权限原则并说明用途。
- 写入外部文档、发送消息、上传文件和修改权限必须得到明确授权。
- 服务异常不得向用户暴露堆栈、错误码或内部术语。

## 11. 禁止事项

- 禁止重新引入旧 HTML 原型、Stitch 参考原型或废弃工作树代码。
- 禁止引入 OOTD 日历、每日穿搭照片记录和 3:4 分享卡片。
- 禁止把试穿改回固定“上身/下身/脚”模板；当前方案是自由画板。
- 禁止让 AI 从具体衣橱自动选衣。
- 禁止提供 MVP 手动切换城市。
- 禁止未经确认提交或推送。

## 12. 任务结束协议

1. 运行必要验证。
2. 产品事实变化时更新 `PRODUCT.md`；设计规范变化时更新 `DESIGN.md`；实施状态变化时更新 `PROGRESS.md`。
3. 向用户说明修改、验证、未完成事项和风险。
4. 准备提交或推送时再次请求确认。
