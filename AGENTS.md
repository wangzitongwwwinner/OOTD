# 今天穿什么｜Agent 全局工作规范

> 本文件适用于本仓库中的所有 Agent。目标是在保证产品一致性和交付质量的前提下，减少重复探索、无效输出、Token 浪费和未经授权的变更。

## 1. 启动协议

开始任何任务前，按任务类型最小化读取：

1. 所有 Agent 必读本文件。
2. 产品、需求、业务规则任务：读取 [PRODUCT.md](./PRODUCT.md)。
3. UI、交互、原型、视觉任务：读取 [PRODUCT.md](./PRODUCT.md) 和 [DESIGN.md](./DESIGN.md)。
4. 开发、测试、排障任务：读取 [PRODUCT.md](./PRODUCT.md) 相关章节和 [PROGRESS.md](./PROGRESS.md) 当前状态；仅在涉及 UI 时读取 `DESIGN.md`。
5. 任务规划、状态汇报、接力任务：读取 [PROGRESS.md](./PROGRESS.md)。

不要每次无差别读取所有文档。先判断任务，再读取必要来源。

## 2. 信息源优先级

遇到冲突时按以下顺序处理：

1. 用户当前对话中的最新明确指令。
2. 飞书 PRD 第十八章 v1.1 已确认修订。
3. [PRODUCT.md](./PRODUCT.md) 中已固化的最新产品事实。
4. [DESIGN.md](./DESIGN.md) 中已确认的 UI 与交互规范。
5. [PROGRESS.md](./PROGRESS.md) 中的当前实施状态和决策记录。
6. 飞书 PRD v1.0 其他章节。
7. 当前代码或原型表现。
8. Agent 自身推测。

代码实现不等于产品事实。发现代码与高优先级文档不一致时，应报告并按任务授权范围处理，不得反向修改文档迎合错误实现。

## 3. 文档职责

- `PRODUCT.md`：稳定产品事实，不记录每日执行流水。
- `DESIGN.md`：原型/UI/交互规范与设计完成度。
- `PROGRESS.md`：里程碑、当前进度、阻塞、决策和下一步。
- `AGENTS.md`：全局协作原则和工作规范。

同一事实只在一个主文档中完整定义，其他文档使用相对链接引用。禁止复制粘贴大段相同内容导致多版本漂移。

## 4. Agent 角色边界

### 4.1 产品 Agent

- 维护需求边界、业务规则、验收标准和优先级。
- 识别 PRD 冲突和遗漏，提出问题与可选方案。
- 产品决策确认后更新 `PRODUCT.md`；执行进度更新 `PROGRESS.md`。
- 不擅自决定视觉细节或技术实现。

### 4.2 UI/原型 Agent

- 严格依据 `PRODUCT.md` 与 `DESIGN.md` 工作。
- 优先复用已有视觉系统和组件，不因个人偏好重做布局。
- 未经确认不得改变信息架构、功能范围、页面顺序或产品文案含义。
- 视觉决策确认后更新 `DESIGN.md`，进度更新 `PROGRESS.md`。

### 4.3 前端 Agent

- 将确认后的原型实现为可维护、可测试的界面。
- 区分 HTML 交互原型与最终微信小程序代码，不把原型实现误当生产架构。
- 修改行为前先补测试；修改纯视觉时至少运行现有回归测试和格式检查。
- 不自行扩展后端、埋点或 AI 能力。

### 4.4 后端/AI Agent

- 依据产品数据对象和接口需求设计云函数、数据结构与服务降级。
- AI 建议必须基于完整行程，不与用户具体衣橱自动绑定。
- 不在日志、前端或提交中暴露密钥、Token、用户隐私和精确定位。
- LLM、天气和抠图服务都必须有超时、错误和降级策略。

### 4.5 测试 Agent

- 从产品验收标准生成测试，不仅验证当前实现。
- 覆盖主链路、边界条件、权限拒绝、API失败和数据同步。
- 对发现的问题给出复现步骤、期望结果、实际结果和严重程度。
- 只诊断时不得顺带修改产品代码。

## 5. 工作原则

### 5.1 先确认范围，再行动

- 用户要求分析、评审或诊断时，只做只读检查和报告，不自动实现。
- 用户要求修改或构建时，在现有范围内直接完成并验证。
- 需要改变产品方向、信息架构、外部服务或数据模型时，先向用户确认。

### 5.2 保留用户已有工作

- 工作区可能包含用户或其他 Agent 的未提交修改。
- 修改前运行 `git status --short`。
- 不重置、不覆盖、不删除与当前任务无关的变更。
- 禁止使用 `git reset --hard`、强制 checkout、强制推送，除非用户明确要求并确认风险。

### 5.3 最小可验证变更

- 每次只解决当前需求，不夹带无关重构。
- 优先复用已有组件、脚本、设计令牌和测试工具。
- 修改后执行与风险匹配的验证，不凭代码阅读宣称完成。
- 失败时报告真实状态，不使用“应该可以”“大概没问题”。

## 6. Token 与资源效率

- 先用 `rg` / `rg --files` 定位，再读取必要片段；不要反复输出整文件。
- 同一个会话中已读取且未变化的文档或技能，不重复读取。
- 只为当前任务调用必要工具；没有必要时不启动浏览器、服务器或外部 API。
- 可并行且互不依赖的只读检查应并行执行。
- 避免生成用户未要求的长计划、重复总结和过程文件。
- Commentary 只报告关键假设、进展和阻塞，不粘贴大量日志。
- 最终回复聚焦结果、验证证据、未完成事项和需要用户决策的下一步。
- 外部工具返回大量内容时先筛选相关部分，不把原始输出完整复制进项目文档。

## 7. 原型开发规范

- 当前 HTML 原型入口：`index.html`。
- 状态和交互逻辑：`app.mjs`。
- 样式：`styles.css`。
- 测试：`app.test.mjs`。
- 本地服务：`server.mjs`。
- 本地预览命令：`node server.mjs`。
- 测试命令：`node --test app.test.mjs`。

实现行为变更时：

1. 写能证明期望行为的失败测试。
2. 确认测试因缺少功能而失败。
3. 写最小实现。
4. 运行完整测试。
5. 运行 `git diff --check`。

纯视觉修改无法用状态测试覆盖时，至少运行现有测试、格式检查，并在可用环境进行页面视觉检查。

## 8. Git 与提交规范

- 默认分支：`main`。
- 用户已明确要求：准备执行 `git commit` 或 `git push` 前必须先询问确认。
- 用户确认后，仅暂存本次约定范围内的文件。
- 不把临时截图、缓存、日志、工具生成的过程文件加入提交。
- 提交信息使用简洁中文，描述单次变更核心。
- 提交前运行测试和 `git diff --check`。
- 推送后核验远端分支指向新提交。
- 不自动创建 PR、Tag、Release 或部署，除非用户明确要求。

## 9. 外部服务与权限

- 读取飞书文档优先使用已安装的 `lark-cli`，并遵循其内置 skill。
- 写入飞书、发送消息、上传文件、修改权限等外部副作用必须得到明确授权。
- 不把 App Secret、Access Token、设备码、个人信息写入仓库、日志或回复。
- 对定位、相册、相机等敏感权限遵循最小权限原则。

## 10. 禁止事项

- 禁止把旧 PRD 的固定三层试穿模板重新引入；当前方案是自由留白画板。
- 禁止把 AI 建议改为从具体衣橱自动挑选衣物；此功能不在 MVP。
- 禁止按单个场景分别生成建议；必须综合完整行程。
- 禁止建议直筒、修身等版型；建议只表达衣物类别和薄厚。
- 禁止 MVP 手动切换城市。
- 禁止在 MVP 中重新引入 OOTD 日历、每日穿搭照片记录或 3:4 分享卡片。
- 禁止大面积黑色、无意义英文、过度动画和与功能无关的视觉装饰。
- 禁止把代码现状当作高于产品文档的需求来源。
- 禁止在未验证时宣称测试通过或任务完成。
- 禁止未经用户确认提交或推送。
- 禁止为“看起来完整”而虚构接口、数据、指标结果或用户反馈。

## 11. 任务结束协议

每次完成有效工作后：

1. 运行必要验证。
2. 更新 [PROGRESS.md](./PROGRESS.md) 中与本任务有关的状态；只有产品事实变化时才改 `PRODUCT.md`，只有设计规范变化时才改 `DESIGN.md`。
3. 向用户说明修改内容、验证结果和未完成事项。
4. 如准备提交，先询问用户确认。

## 12. 发现冲突时的处理模板

简洁报告：

```text
发现冲突：<冲突内容>
来源 A：<高优先级来源>
来源 B：<低优先级来源>
建议处理：<方案与影响>
是否需要用户决策：是/否
```

确认后的结果写入 `PRODUCT.md` 或 `DESIGN.md`，并在 `PROGRESS.md` 决策记录中留痕。
