# 今天穿什么｜原型与 UI 设计规范

> 本文档是原型 UI 的唯一设计要求。产品范围以 [PRODUCT.md](./PRODUCT.md) 为准，开发与 Agent 规则见 [AGENTS.md](./AGENTS.md)，当前完成度见 [PROGRESS.md](./PROGRESS.md)。

## 1. 设计目标

为每天早上快速做穿衣决策的城市通勤用户，设计一个简约、优雅、轻盈，同时具有时尚辨识度的微信小程序。界面要让“天气—建议—行程”的关系一眼可理解，不制造额外决策负担。

关键词：

- 简约优雅
- 编辑式排版
- 潮流但克制
- 清晰、轻量、有呼吸感
- 中文优先

禁止把界面做成大面积黑色、霓虹科技感、复杂渐变堆叠、过度卡通或模板化 AI Dashboard。

## 2. 当前原型入口

- 直接打开：[index.html](./index.html)
- 本地文件地址：<file:///G:/24%E7%BA%A7%E7%8E%8B%E6%A2%93%E6%A1%90/OOTDapp/index.html>
- 本地服务器：运行 `node server.mjs`，访问 <http://127.0.0.1:4173>
- 原型代码：`index.html`、`styles.css`、`app.mjs`
- 交互测试：`node --test app.test.mjs`

## 3. 参考风格与取舍

### 3.1 风格来源

- 小红书 / Nice 的年轻、时尚与内容感。
- 现代杂志 Editorial 的留白、字体层级与图文节奏。
- 参考过的植物应用界面：宋体大标题、细线图标、冷白底、低饱和彩色主视觉。

### 3.2 不照搬的内容

- 不复制参考产品的信息结构。
- 不使用大量英文装饰；面向用户的信息全部使用中文。
- 不为了杂志感牺牲可读性和交互效率。
- 不添加与功能无关的编号、贴纸、装饰线或动画。

## 4. 视觉系统

### 4.1 字体

- 展示标题：`Songti SC`, `STSong`, `Noto Serif SC`, serif。
- 正文与控件：`PingFang SC`, `Microsoft YaHei`, system-ui, sans-serif。
- 页面大标题、卡片标题、月份标题可使用现代宋体。
- 天气数据、行程、按钮、标签使用无衬线体。
- 字重以 400/500/700 为主，避免所有内容都粗体。

### 4.2 字体层级

从大到小：

1. 页面标题与核心天气数字。
2. 穿衣建议核心内容。
3. 卡片标题、章节标题。
4. 解释文本、行程信息。
5. 辅助标签、导航文字。

建议核心必须是建议卡中最醒目的文字。解释和小贴士不能与核心建议抢层级。

### 4.3 色彩

当前设计令牌方向：

- 页面背景：珍珠冷白 `#fafbfc`。
- 页面外背景：雾灰 `#eef0f4`。
- 主文字：墨黑 `#1b1d24`。
- 次级文字：灰紫 `#747782`。
- 天气卡：浅天蓝渐变。
- 建议卡：浅天空蓝 `#bfe2ff`，是首页核心视觉。
- 选中/辅助：雾紫 `#c9c3ed` 或更浅的紫色。
- 强调点缀：克制珊瑚色，不作大面积背景。

色彩使用原则：大面积低饱和，少量高饱和点缀。页面同时出现的主色不超过三组。

### 4.4 圆角、边框与阴影

- 主卡片圆角约 20–24px。
- 小控件圆角约 9–12px，不把所有按钮做成胶囊。
- 优先使用 1px 低对比边框表达层级。
- 阴影只用于浮层、底部导航和必要的空间层级，避免厚重悬浮感。

### 4.5 图标

- 使用统一的细线 SVG 风格。
- 底部五个 Tab 必须都有图标，图标视觉大小一致。
- 天气可使用 CSS 太阳、云朵和半透明气流环。
- 建议卡右上为衣物/生成建议图标，不使用机器人宠物。

## 5. 全局布局

- 移动端设计基准宽度约 390–430px。
- 内容区左右边距约 18–24px。
- 底部导航固定，五等分铺满可用宽度。
- 页面内容必须预留导航和安全区空间。
- 关键信息应在首屏尽量完成“天气 + 建议”的阅读。
- 页面信息和操作顺序不可仅为视觉效果而重排。

## 6. 页面要求

### 6.1 登录页

信息：

- 品牌名“今天穿什么”。
- 天空插画主卡。
- 价值文案：“记住你的场景温度，每天少一点穿衣纠结。”
- 微信一键登录按钮。
- 用户协议与隐私政策提示。

视觉：

- 主卡使用天空蓝、太阳和云朵。
- 价值文案使用宋体或具有编辑感的标题字体。
- 登录按钮遵循微信语义，但整体形态保持克制。

### 6.2 首页

固定顺序：天气卡 → 穿衣建议卡 → 今天行程。

#### 天气卡

- 位置、日期、当前温度、天气、体感、湿度、最高/最低温均在卡片内。
- 天气插画可略微超出卡片，但不能遮挡数据。
- 温度是卡片最大视觉元素。

#### 建议卡

- 标题：“今天建议这样穿”，不使用胶囊框。
- 提供“生成穿衣建议”入口；生成状态需有加载反馈，避免重复点击。
- “生成穿衣建议”入口位于建议卡右上角，与衣服图标合并为胶囊按钮：左侧为缩小后的衣服细线图标，右侧文字为“生成穿衣建议”。文字必须醒目、可读，不做小号辅助标签。
- 该入口属于整张全天建议卡，不属于单条行程；视觉上必须表达“综合完整行程后生成建议”，避免用户理解为按某个场景单独生成。
- 不展示模型置信度。
- 不展示“晴朗天气 · 午间炎热 · 室内空调偏冷”摘要行。
- 核心建议只使用衣物类型和薄厚，例如“短袖 + 薄开衫 + 短裤”。
- 解释文案加粗关键温度与可穿脱策略。
- 小贴士使用低对比内层卡片。
- 三个反馈按钮带表情，选中态为边框高亮，不使用黑底。
- 建议卡使用浅天空蓝，是首页核心卡片。
- 天气卡的半透明椭圆环应连续进入建议卡，表达两者的数据关联。

#### 今天行程

- 标题右侧提供“添加”按钮。
- 添加操作使用底部弹层；弹层应贴近手机容器底部并在原型手机框内居中，不得脱离手机框或偏移到页面右侧。
- 添加行程弹层字段为：场景、温度、开始时间、持续时间。
- 场景不支持自由输入。用户只能从常用场景中选择，或点击“新建场景”进入二级卡片。
- 常用场景以横向/换行标签呈现，选中态需明确；选择场景后可带出默认温度，用户仍可在本次行程中调整。
- 新建场景使用独立二级卡片，填写“场景名称 + 默认温度 + 体感”。保存后新场景自动同步到场景库，并回填到当前添加行程弹层。
- 每条行程展示：时间、表情+场景、温度、停留时长、最右侧删除按钮。
- 各信息应尽量铺满一行，避免右侧大量空白。
- 删除按钮需有明确可点击区域，但视觉权重低于行程信息。

### 6.3 场景库

- 页面标题与说明：“常去的地方，记住大概温度就好”。
- 场景卡展示表情、名称、约多少度、体感标签。
- 场景卡使用竖向排列的条形列表，不使用正方形宫格：左侧为场景图标和名称、温度、体感信息，右侧并列提供编辑、删除按钮；信息优先于操作按钮。
- 从场景库直接新建场景时，弹层右上角为关闭按钮，点击后直接关闭；仅从“添加行程”进入的新建场景弹层保留返回行程的入口。
- 温度输入必须提示“估计即可”。
- 支持新增、编辑和删除。
- 场景卡色彩低饱和、彼此可区分，但不做糖果色堆叠。

### 6.4 衣橱

- 页面标题“我的衣橱”。
- 筛选栏上方提供胶囊圆角的衣物名称模糊搜索框。
- 类别和颜色在同一行以胶囊式下拉选择器呈现；可组合筛选。类别预设全部、短袖、衬衫、毛衣、裤子、鞋子、外套、裙装；颜色预设全部、黑、白、灰、蓝、橙、红、绿、棕。
- 衣物以实物平铺预览图为主体，三列布局；图片区必须是等宽等高的方形圆角卡片，不得显示为圆形或椭圆形。编辑、删除两个深色半透明按钮固定在右上角并横向排列。
- 衣物卡片只显示衣物照片以及右上角的编辑、删除按钮，不在图片下方展示衣物名称、类别或颜色；名称仅作为图片替代文本和按钮可访问名称保留。
- 上传按钮固定在易触达位置。
- 点击上传按钮后使用底部弹层，提供拍照上传、从相册选择及关闭入口。
- 上传流程需包含拍摄引导、处理中、失败、确认/重拍状态。
- 空状态应引导用户先上传 3 件衣物体验搭配。

### 6.5 试穿

- 标题“自由试穿”。
- 画板完全留白，不显示人体、上身/下身/脚分区或固定模板。
- 衣物从衣橱拖入后支持移动、缩放、层级调整和删除。
- 被选中衣物应有轻量选中框/操作柄。
- 控件不能遮挡画板主要区域。
- 保存后需提供清晰成功反馈。

### 6.6 OOTD 日历

- 一级导航名称固定为“OOTD日历”；导航空间不足时原型可显示“日历”，但页面标题需完整。
- 月历可切换月份。
- 有照片或行程的日期应有可区分标记。
- 日期详情展示当日行程与穿搭照片入口。
- 上传、查看、更换照片的状态清晰。
- 分享卡片入口应从有内容的日期自然进入。

### 6.7 分享卡片

- 固定 3:4 竖版。
- 搭配图或 OOTD 照片为主视觉，约占 70%。
- 使用杂志式排版，但全部用户信息使用中文。
- 包含日期、天气氛围文字和低调品牌署名。
- 不加入二维码。

## 7. 交互状态要求

所有可操作功能至少设计：

- 默认态
- 点击/选中态
- 加载态
- 成功态
- 空状态
- 失败与重试态
- 权限拒绝态（定位、相机、相册）

删除衣物、删除行程等破坏性操作应防误触。影响历史数据的删除必须二次确认；普通未保存行程的删除可轻量确认或支持撤销。

## 8. 文案规范

- 全部用户可见信息优先使用中文。
- 直接说明用户要做什么，不使用技术术语。
- 温度输入强调“大概”“估计即可”。
- 操作名称前后一致，例如按钮“保存搭配”，成功反馈“搭配已保存”。
- 错误提示同时说明原因和下一步，例如“天气获取失败，请检查定位权限后重试”。
- 不使用“AI置信度”“Token”“API失败”等内部语言。

## 9. 响应式与可用性

- 原型需适配常见手机宽度，最窄宽度下不得横向溢出。
- 触控目标建议不小于 40×40px。
- 文字与背景保持清晰对比。
- 键盘焦点应可见。
- 遵守 `prefers-reduced-motion`。
- 插画和图标不承载唯一信息，需有文字或可访问名称。

## 10. 当前原型进度

已实现：

- 微信登录模拟。
- 五个一级页面与底部导航。
- 首页天空天气卡、建议卡、反馈按钮。
- 建议卡“生成穿衣建议”入口与提示反馈。
- 行程新增底部弹层、常用场景选择、新场景同步、行程删除。
- 场景库、衣橱、自由试穿画板和日历的静态/基础交互原型。
- 首页建议入口、行程新增、新场景同步、样式约束等核心原型测试。

仍需设计或深化：

- 真实建议生成的加载、成功、失败和降级状态。
- 场景编辑与温度/体感设置完整流程。
- 衣物上传、拍照引导、抠图确认流程。
- 自由画板层级调整、衣物删除、保存还原。
- OOTD 照片上传与分享卡片完整流程。
- 微信权限拒绝与恢复路径。

详细状态和下一步以 [PROGRESS.md](./PROGRESS.md) 为准。

## 11. 原型验收清单

UI Agent 每轮交付前确认：

- 页面结构与 [PRODUCT.md](./PRODUCT.md) 一致。
- 未擅自新增产品功能或改变信息顺序。
- 首页首屏能理解天气、建议和两者关联。
- 字体、颜色、圆角、图标保持统一。
- 不出现大块黑色、无意义英文和过度装饰。
- 交互状态完整，不只设计静态理想态。
- 手机宽度无横向溢出，底部导航不遮挡内容。
- 更新后运行 `node --test app.test.mjs` 和 `git diff --check`。
