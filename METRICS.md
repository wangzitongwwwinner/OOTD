# 穿衣有数｜MVP 指标口径与开发指引

> 本文是产品指标名称、计算口径、近似边界、数据要求和查看方式的单一来源。开发任务见 [TASK.md](./TASK.md)，系统结构见 [ARCHITECTURE.md](./ARCHITECTURE.md)，测试要求见 [TEST.md](./TEST.md)。

## 1. 目标与范围

- MVP 先观察方向和数量级，不追求审计级精确统计。
- M9-04 建立非评价指标的数据基础和 Markdown/CSV 报告。
- M10-01 在产品开发完成后增加 AI 建议评价卡片及三项评价指标。
- 不建设面向普通用户的运营入口，不把运营指标暴露在小程序页面。
- 不计算“AI 建议请求完成率”。

## 2. 统一统计规则

### 2.1 总用户数

`总用户数`指统计截止时间前成功创建的去重正式用户数。开发账号、自动化测试账号和明确标记的测试数据应排除；MVP 暂不追溯已清理的无效数据。

### 2.2 时间与去重

- 时间戳使用 UTC 存储；自然日和 7 日窗口按 `Asia/Shanghai` 解释。
- 用户类指标按可信 `userId` 去重。
- 建议类指标按稳定 `recommendationId` 去重。
- 报告必须同时展示百分比、分子、分母、统计截止时间和口径版本。
- 分母为 0 时显示 `—`，不得显示 `0%` 或制造假数据。

### 2.3 MVP 近似原则

- 允许使用现有业务表和少量完成标记近似计算，不要求独立全量事件仓库。
- 建议成功记录近似代表建议成功展示；M10-01 前不做精确曝光检测。
- 场景或行程删除可能使基于当前记录的累计完成率偏低，报告必须注明该限制。
- 评价类指标在 M10-01 前显示“暂不可用”。

## 3. 指标总览

| 指标               | 计算公式                                                                                        | MVP 可用阶段 |
| ------------------ | ----------------------------------------------------------------------------------------------- | ------------ |
| AI 建议有用率      | “有帮助”的有效评价建议数 ÷ 已评价有效建议数                                                     | M10-01 后    |
| AI 建议正向反馈率  | （“有帮助”＋“一般”）的有效评价建议数 ÷ 已评价有效建议数                                         | M10-01 后    |
| AI 建议评价覆盖率  | 已完成评价的有效建议数 ÷ 建议成功展示数                                                         | M10-01 后    |
| AI 建议功能复用率  | 首次成功生成后 7 天内至少再次成功生成一次的用户数 ÷ 已获得完整 7 天观察窗口的首次成功生成用户数 | M9-04 后     |
| AI 建议用户使用率  | 至少成功使用过一次 AI 建议的用户数 ÷ 总用户数                                                   | M9-04 后     |
| 场景填写完成率     | 至少成功完成过一次有效场景编辑的用户数 ÷ 总用户数                                               | M9-04 后     |
| 行程设置完成率     | 至少成功保存过一段有效行程的用户数 ÷ 总用户数                                                   | M9-04 后     |
| 个人体感设置修改率 | 至少成功完成过一次体感设置修改的用户数 ÷ 总用户数                                               | M9-04 后     |

## 4. 详细指标口径

### 4.1 AI 建议有用率

```text
有用率 = rating=helpful 的去重建议数 ÷ 已评价的去重建议数
```

- `rating` 枚举：`helpful`（有帮助）、`neutral`（一般）、`unhelpful`（没帮助）。
- 同一用户对同一 `recommendationId` 只保留一份有效评价。
- 未评价、保存失败和已作废建议不进入评价分母。

### 4.2 AI 建议正向反馈率

```text
正向反馈率 = rating in (helpful, neutral) 的去重建议数 ÷ 已评价的去重建议数
```

数据范围、去重规则与 AI 建议有用率一致。

### 4.3 AI 建议评价覆盖率

```text
评价覆盖率 = 存在有效评价的去重 recommendationId 数 ÷ 成功展示的去重 recommendationId 数
```

MVP 将成功生成并返回首页的建议近似视为成功展示；如果后续需要精确曝光，再增加客户端渲染确认事件，不追溯改写历史数据。

### 4.4 AI 建议功能复用率

```text
7 日复用率 = 首次成功生成后的 (T0, T0+7天] 内至少再次成功生成一次的用户数
             ÷ 截止统计时已拥有完整 7 天观察窗口的首次成功生成用户数
```

- `T0` 是用户第一次成功建议记录时间。
- 同一次请求重试或幂等重放不算再次生成。
- 统计截止时距离 `T0` 不满 7 天的用户不进入分母。

### 4.5 AI 建议用户使用率

```text
AI 建议用户使用率 = 至少存在一次成功建议记录的去重用户数 ÷ 总用户数
```

本指标不再使用“AI 建议生成完成率”名称，也不计算请求级完成率。

### 4.6 场景填写完成率

```text
场景填写完成率 = 至少成功完成过一次有效场景编辑的去重用户数 ÷ 总用户数
```

- “编辑”指对已有场景提交有效修改并成功保存，不包含仅打开编辑弹层、保存失败或内容无变化。
- 预设场景和自定义场景的成功编辑都计入，因此能覆盖用户对初始预设场景的个性化调整。
- 新建场景不自动视为“编辑已有场景”；若未来需要衡量场景创建，应另设指标。

### 4.7 行程设置完成率

```text
行程设置完成率 = 至少成功保存过一段有效行程的去重用户数 ÷ 总用户数
```

新增行程成功即可计入；仅打开表单、校验失败、接口失败或取消不计入。

### 4.8 个人体感设置修改率

```text
个人体感设置修改率 = 至少成功完成过一次体感设置修改的去重用户数 ÷ 总用户数
```

- 必须是用户主动修改且服务端保存成功。
- 初始默认值不计入。
- 修改后又改回默认值仍计入，因为用户确实完成过修改。

## 5. MVP 数据要求

优先复用业务数据，并增加低成本累计标记，避免为了近似指标建设复杂事件平台。

### 5.1 用户累计标记

建议在用户资料中增加：

```text
firstSceneEditedAt?: datetime
firstItinerarySavedAt?: datetime
firstFeelPreferenceModifiedAt?: datetime
isTestUser?: boolean
```

- 只在对应业务操作首次成功时写入，之后不得清空或回退。
- 写入失败不得伪造成功；应与业务成功写入处于同一受控服务端流程。
- `isTestUser=true` 的用户不进入正式报告。

### 5.2 建议成功记录

`recommendationSnapshots` 至少保留：

```text
recommendationId
userId
source
createdAt
```

重复请求必须通过幂等键避免制造多条成功记录。规则降级结果也是用户成功使用建议，计入用户使用率与复用率；报告可按 `source=llm|rules` 另行拆分，但不得改变主指标分母。

### 5.3 建议评价记录（M10-01）

建议集合：`recommendationFeedbacks`。

```text
id
recommendationId
userId
rating: helpful | neutral | unhelpful
createdAt
updatedAt
version
```

- 唯一约束：`userId + recommendationId`。
- 服务端从可信身份获得 `userId`，不得接受客户端指定评价所有者。
- 重复提交需幂等；若允许修改评价，更新原记录而非新增计数。

## 6. 报告与查看方式

M9-04 提供按需执行的统计能力，默认输出：

- Markdown：适合项目复盘和由 Agent 直接展示。
- CSV：适合 Excel、飞书表格或后续可视化。
- CloudBase 中可选保存 `metricSnapshots`，用于保留不同统计日期的趋势快照。

报告示例：

| 指标               | 当前值 | 分子 / 分母 | 状态          |
| ------------------ | -----: | ----------: | ------------- |
| AI 建议用户使用率  |  62.0% |    62 / 100 | 可用          |
| AI 建议功能复用率  |  28.0% |     14 / 50 | 可用          |
| 场景填写完成率     |  56.0% |    56 / 100 | 可用          |
| 行程设置完成率     |  48.0% |    48 / 100 | 可用          |
| 个人体感设置修改率 |  41.0% |    41 / 100 | 可用          |
| AI 建议有用率      |      — |           — | M10-01 后可用 |

MVP 不开发独立运营后台。项目所有者可让 Agent 运行统计并查看 Markdown 报告，或下载 CSV；用户量和长期分析需求明确后，再单独评审图形化后台。

## 7. 开发顺序

### M9-04｜MVP 指标数据基础与报告

1. 固化指标常量、统计时间与测试用户排除规则。
2. 为场景编辑、行程保存和体感修改增加首次成功标记。
3. 校验建议成功记录能按用户和时间去重。
4. 实现纯统计函数和固定夹具测试。
5. 实现只读统计入口与 Markdown/CSV 输出。
6. 用脱敏测试数据核对分子、分母、0 分母和 7 日窗口。

### M10-01｜AI 穿衣建议评价卡片（暂缓）

1. 用户确认卡片位置、触发时机、修改规则和反馈表现。
2. 先建立评价契约、唯一约束、鉴权和幂等测试。
3. 实现三选一评价卡片及加载、成功、失败恢复状态。
4. 将三项评价指标接入同一报告。
5. 完成真机、重复点击、弱网、修改评价和统计回归。

## 8. 隐私与安全

- 指标数据只使用内部可信 `userId`，报告不得输出 OpenID、昵称、头像或可识别个人的信息。
- 不记录建议全文、精确位置、图片 URL、Token、密钥和请求正文。
- 统计入口必须只读并限制为管理员或本地授权流程，不能暴露给普通小程序用户。
- 对外分享报告前必须移除原始用户标识和小样本明细。
